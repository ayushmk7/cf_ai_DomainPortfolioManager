{
  "name": "domain-portfolio-manager",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev -w domain-pilot-frontend",
    "dev:backend": "npm run dev -w domain-pilot-backend",
    "build": "npm run build -w domain-pilot-frontend && npm run typecheck -w domain-pilot-backend",
    "test": "npm run test -w domain-pilot-backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "firebase": "^12.9.0"
  }
}
